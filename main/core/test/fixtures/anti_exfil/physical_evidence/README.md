# Physical evidence corpus

Physical QR images, serial logs, and board receipts are conformance evidence,
not inputs to the semantic-record tests. They remain in the shared reference
workspace and are indexed by its physical checkpoint manifests. This directory
exists to preserve that layer boundary when evidence is later imported.
